#include "render-engine.h"
// #include <U8glib.h>

void initializeRender(RenderEngine *renderEngine) {
    //renderEngine->display->device = u8g(U8G_I2C_OPT_NONE|U8G_I2C_OPT_FAST);
    //renderEngine->display->width = renderEngine->display->device.getWidth();
    //renderEngine->display->height = renderEngine->display->device.getHeight();
}